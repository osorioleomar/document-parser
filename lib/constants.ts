/** Max upload size for parse API (bytes). */
export const MAX_UPLOAD_BYTES = 50 * 1024 * 1024;

/** Max stdout captured from `lit parse` (bytes). */
export const MAX_PARSE_OUTPUT_BYTES = 80 * 1024 * 1024;
